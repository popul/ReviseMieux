# Flow: 02-capture
# ACs: Z2-AC01, Z2-AC10
appId: com.revisemieux.app
---

- launchApp


- extendedWaitUntil:
    timeout: 15000
    visible: ".*Hugo.*"

# Aller sur le tab Capturer (bottom tab bar)
- tapOn: ".*apturer.*"

- extendedWaitUntil:
    timeout: 5000
    visible: ".*"
